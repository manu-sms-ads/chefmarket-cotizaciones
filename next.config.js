/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // bodyParser se desactiva por ruta en pages/api/submit.js con export const config
};

module.exports = nextConfig;
